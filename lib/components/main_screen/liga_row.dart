import 'package:flutter/material.dart';

class LigaRow extends StatelessWidget {
const LigaRow({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}